import 'package:flutter/material.dart';

class DialogPage extends StatelessWidget {
  @override
  DialogPageState build(BuildContext context) => DialogPageState();
}

class DialogPageState extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: new AppBar(
        title: Text("dialog"),
      ),
      body: new Center(
        child: Column(
          children: <Widget>[],
        ),
      ),
    );
  }
}
