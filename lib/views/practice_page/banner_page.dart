import 'package:flutter/material.dart';

class BannerPage extends StatelessWidget {
  @override
  BannerPageState build(BuildContext context) => BannerPageState();
}

class BannerPageState extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: new AppBar(
        title: Text("banner"),
      ),
      body: new Center(
        child: Column(
          children: <Widget>[],
        ),
      ),
    );
  }
}
