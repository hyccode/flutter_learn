import 'package:flutter/material.dart';

class AnimalPage extends StatelessWidget {
  @override
  AnimalPageState build(BuildContext context) => AnimalPageState();
}

class AnimalPageState extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: new AppBar(
        title: Text("animal"),
      ),
      body: new Center(
        child: Column(
          children: <Widget>[],
        ),
      ),
    );
  }
}
